  <h2> ジャンル編集</h2>
    <%= form_for @genre, url:admin_genre_path do |f| %>
      <strong> ジャンル</strong>
      <p><%= f.text_field :genre, class:"form-control" %></p>
      <%= f.hidden_field :movie_id, :value => @movie.id %>
      <%= f.submit 'update genre', id:"back" %>
    <% end %>