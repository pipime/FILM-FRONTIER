
  <%= form_for @genre, url: admin_genres_path do |f| %>

      <h1> ジャンル追加</h1>

          <%= f.text_field :genre %>

          <%= f.submit "add" %>

  <% end %>

      <h1> ジャンル一覧</h1>

  <% @genres.each do |genre| %>

        <p><%= genre.genre %></p>

  <% end %>